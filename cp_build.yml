name: moderntreasury-client

tools:
  jdk: 13.0.2-zulu13.29.9

test:
  stages:
    unit:
      script: ./jenkins_build.sh
      junit: "*/build/test-results/test/TEST-*.xml"

build:
  script: ./gradlew shadowJar
  docker: false

publish:
  tag:
    library: java/gradle
